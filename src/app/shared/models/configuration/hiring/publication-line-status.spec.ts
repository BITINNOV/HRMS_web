import {PublicationLineStatus} from './publication-line-status';

describe('PublicationLineStatus', () => {
  it('should create an instance', () => {
    expect(new PublicationLineStatus()).toBeTruthy();
  });
});
