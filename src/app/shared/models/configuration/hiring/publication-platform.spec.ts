import { PublicationPlatform } from './publication-platform';

describe('PublicationPlatform', () => {
  it('should create an instance', () => {
    expect(new PublicationPlatform()).toBeTruthy();
  });
});
