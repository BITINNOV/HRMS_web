import {IntegrationCycleLineStatus} from './integration-cycle-line-status';

describe('IntegrationCycleLineStatus', () => {
  it('should create an instance', () => {
    expect(new IntegrationCycleLineStatus()).toBeTruthy();
  });
});
