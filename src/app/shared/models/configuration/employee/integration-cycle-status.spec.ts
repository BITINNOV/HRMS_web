import {IntegrationCycleStatus} from './integration-cycle-status';

describe('IntegrationCycleStatus', () => {
  it('should create an instance', () => {
    expect(new IntegrationCycleStatus()).toBeTruthy();
  });
});
