import {Organization} from '../organization';

export class DocumentTerms {
  id: number;
  // document: Document;
  code: String;
  description: String;
  number: number;
  organization: Organization;
}
