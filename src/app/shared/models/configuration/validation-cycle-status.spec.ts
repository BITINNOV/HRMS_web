import { ValidationCycleStatus } from './validation-cycle-status';

describe('ValidationCycleStatus', () => {
  it('should create an instance', () => {
    expect(new ValidationCycleStatus()).toBeTruthy();
  });
});
