import { ValidationCycleLineStatus } from './validation-cycle-line-status';

describe('ValidationCycleLineStatus', () => {
  it('should create an instance', () => {
    expect(new ValidationCycleLineStatus()).toBeTruthy();
  });
});
